// RON serialization is handled directly on SceneBuilder via serde derives.
// This module exists as an extension point for additional format support
// (e.g. JSON, custom binary formats, etc.) in the future.
