when:
  - event: tag
    ref: refs/tags/v*

matrix:
  TARGET:
    - x86_64-unknown-linux-gnu
    - aarch64-unknown-linux-gnu

steps:
  format:
    image: nixos/nix:latest
    environment:
      SKIP_GLTF_EXPORT: "true"
    commands:
      - |
        DEV_SHELL=""
        [ "$TARGET" = "aarch64-unknown-linux-gnu" ] && DEV_SHELL=".#aarch64-cross"
        nix develop --extra-experimental-features 'nix-command flakes' $DEV_SHELL --command cargo fmt --all --check

  clippy:
    image: nixos/nix:latest
    environment:
      SKIP_GLTF_EXPORT: "true"
    commands:
      - |
        DEV_SHELL=""
        [ "$TARGET" = "aarch64-unknown-linux-gnu" ] && DEV_SHELL=".#aarch64-cross"
        nix develop --extra-experimental-features 'nix-command flakes' $DEV_SHELL --command cargo clippy

  test:
    image: nixos/nix:latest
    environment:
      SKIP_GLTF_EXPORT: "true"
    commands:
      - |
        DEV_SHELL=""
        [ "$TARGET" = "aarch64-unknown-linux-gnu" ] && DEV_SHELL=".#aarch64-cross"
        nix develop --extra-experimental-features 'nix-command flakes' $DEV_SHELL --command cargo test --no-fail-fast

  build:
    image: nixos/nix:latest
    environment:
      SKIP_GLTF_EXPORT: "true"
    commands:
      - |
        DEV_SHELL=""
        [ "$TARGET" = "aarch64-unknown-linux-gnu" ] && DEV_SHELL=".#aarch64-cross"
        nix develop --extra-experimental-features 'nix-command flakes' $DEV_SHELL --command cargo build --release --target "$TARGET"

  package:
    image: nixos/nix:latest
    commands:
      - mkdir upload
      - find "target/$TARGET/release" -mindepth 1 -maxdepth 1 -type f -not -name '*.d' -not -name '*.rlib' -not -name '.cargo-lock' -not -name 'CACHEDIR.TAG' -exec mv {} upload/ \;
      - mv Assets/ upload/
      - cd upload && zip -r "../$TARGET.zip" .

  publish:
    image: nixos/nix:latest
    environment:
      FORGEJO_USERNAME:
        from_secret: forgejo_username
      FORGEJO_PASSWORD:
        from_secret: forgejo_password
    commands:
      - |
        REPO_URL=$(git remote get-url origin)
        BASE_URL=$(echo "$REPO_URL" | cut -d/ -f1-3)
        API_URL="$BASE_URL/api/v1/repos/$CI_REPO_OWNER/$CI_REPO_NAME"
        RESP=$(curl -s -u "$FORGEJO_USERNAME:$FORGEJO_PASSWORD" "$API_URL/releases/tags/$CI_TAG")
        RELEASE_ID=$(echo "$RESP" | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2)
        if [ -z "$RELEASE_ID" ]; then
          RESP=$(curl -s -X POST \
            -u "$FORGEJO_USERNAME:$FORGEJO_PASSWORD" \
            -H "Content-Type: application/json" \
            -d "{\"tag_name\": \"$CI_TAG\", \"name\": \"$CI_TAG\", \"body\": \"Release $CI_TAG\", \"draft\": false, \"prerelease\": false}" \
            "$API_URL/releases")
          RELEASE_ID=$(echo "$RESP" | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2)
        fi
        if [ -n "$RELEASE_ID" ]; then
          curl -s -X POST \
            -u "$FORGEJO_USERNAME:$FORGEJO_PASSWORD" \
            -H "Content-Type: application/octet-stream" \
            --data-binary "@$TARGET.zip" \
            "$API_URL/releases/$RELEASE_ID/assets?name=$TARGET.zip"
          echo "Published $TARGET.zip to release $CI_TAG"
        else
          echo "Failed to get or create release"
          exit 1
        fi
